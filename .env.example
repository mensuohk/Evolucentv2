# Neon DB
# Pooled connection for your application
DATABASE_URL="postgresql://USER:PASSWORD@EP-pooler.REGION.aws.neon.tech/DB_NAME?sslmode=require"
# Direct connection for Prisma CLI workflows (migrations, db push, introspection)
DIRECT_URL="postgresql://USER:PASSWORD@EP.REGION.aws.neon.tech/DB_NAME?sslmode=require"

# Auth.js — generate with: npx auth secret
AUTH_SECRET=

# App URL — LOCAL DEV ONLY. Do NOT set this in production.
# NextAuth v5 auto-detects the correct URL from the Host header when AUTH_URL is absent.
# Setting AUTH_URL=http://localhost:3000 in production will redirect all Google sign-ins to localhost.
# AUTH_URL=http://localhost:3000

# Google OAuth (also supported: GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Paystack Payment Gateway
PAYSTACK_SECRET_KEY=
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=

# GhanaNLP Khaya TTS
GHANANLP_API_KEY=
GHANANLP_API_KEY_SECONDARY=

# Google Gemini (AI Impact Summaries + multi-language translations + civic feed enrichment)
GOOGLE_AI_API_KEY=
# Optional: override the Gemini model (default: gemini-1.5-flash)
# GOOGLE_AI_MODEL=gemini-1.5-pro